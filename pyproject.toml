[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "python-cli"
version = "0.1.0"
description = "Example Python CLI project"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
